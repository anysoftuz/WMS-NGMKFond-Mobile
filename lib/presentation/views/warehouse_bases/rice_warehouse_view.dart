import 'package:flutter/material.dart';

class RiceWarehouseView extends StatelessWidget {
  const RiceWarehouseView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}